import {TableCell} from "@mui/material";
import React from "react";

const Club = ({club}) => {

    return (
        <React.Fragment>
            <TableCell></TableCell>
        </React.Fragment>
    )
}

export default Club;
