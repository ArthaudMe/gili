import {TableCell} from "@mui/material";
import React from "react";

const Member = ({member}) => {

    return (
        <React.Fragment>
            <TableCell></TableCell>
        </React.Fragment>
    )
}

export default Member;
